apiVersion: v1
kind: Service
metadata:
  labels:
    openstack-app: messaging
  name: messaging
  namespace: os-messaging
spec:
  clusterIP: None
  sessionAffinity: ClientIP
  ports:
  - port: 443
    protocol: TCP
    targetPort: 9696
  selector:
    openstack-app: messaging
