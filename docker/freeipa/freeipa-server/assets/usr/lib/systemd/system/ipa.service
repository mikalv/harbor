[Unit]
Description=Identity, Policy, Audit
Requires=network.target
After=network.target

[Service]
Restart=always
RestartSec=20
Type=simple
ExecStart=/usr/sbin/ipactl restart
ExecStop=/usr/sbin/ipactl stop
StandardOutput=tty
RemainAfterExit=yes
TimeoutSec=0

[Install]
WantedBy=multi-user.target
